using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Collectibles : MonoBehaviour
{

    //TO-DO
    //Check for player collision with gameObject
    //If true based on Objects's sprite colour(R,G or B) add +25 to player's sprite respective R,G,B channel 


    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
